"""
RAG Service

Manages the pgvector-backed retrieval-augmented generation pipeline.
  - embed_document(content, metadata)         — embed and store a document
  - similarity_search(query, k, filter)       — cosine similarity search
  - ingest_malay_corpus()                     — seed vector store on first startup
"""
# TODO: Implement in Phase 3
